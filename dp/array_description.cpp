#include<iostream>
#include<vector>
using namespace std;
int solve(vector<int> a)
{
    for(int i=0; i<a.size(); ++i)
    {
        if(a[i]==0)
        
    }
}
int main()
{
    int n, m;
    cin>>n>>m;
    vector<int> a(n);
    for(int i=0; i<n; ++i)
        cin>>a[i];
        cout<<solve(a);
    return 0;
}