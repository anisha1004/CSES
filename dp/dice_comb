#include<iostream>
using namespace std;

int solve(int n)
{
    if(n==0)
        return 0;
    if(n==1)
        return 1;
    int ans=0;
    for(int i=1; i<=6; ++i)
    {
        ans+=solve(n-i);
    }
    return ans;
}
int main()
{
    int n;
    cin>>n;
    cout<<solve(n);
    return 0;
}